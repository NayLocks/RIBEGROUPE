{% extends 'base.html.twig' %}

{% block title %}Message
{% endblock %}
{% block body %}
	<div class="row">
		<div class="col-md-4"></div>
		<div class="col-md-4">
			<div class="mb-3 text-center card card-body">
				<h5 class="card-title">{{ titleMessage }}</h5> {{ message }}
				<br>
				<a class="btn btn-danger" href="{{ path("index") }}">Retourner à l'accueil</a>
			</div>
		</div>
		<div class="col-md-4"></div>
	</div>
{% endblock %}
